{
  "name": "dnsctl-nix",
  "private": true,
  "type": "module",
  "dependencies": {
    "cloudflare": "^4.0.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/main.cjs --minify --tree-shaking=true --legal-comments=none",
    "check": "pnpm run typecheck && pnpm run build",
    "typecheck": "tsc --noEmit"
  }
}
